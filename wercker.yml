box: phpmentors/symfony-app
build:
  steps:
    - script:
      name: install composer
      code: curl -sS https://getcomposer.org/installer | php
    - script:
      name: install dependencies
      code: php composer.phar install
    - script:
      name: run unit tests
      code: phpunit